---
phase: 04-live-race-experience
plan: "01"
subsystem: ui
tags: [activitykit, widgetkit, swiftui, dynamic-island, live-activities, xcodegen]

# Dependency graph
requires:
  - phase: 03-client-feature-surface
    provides: F1AIWidgets WidgetBundle with NextRaceWidget already registered

provides:
  - RaceLiveActivityAttributes ActivityAttributes struct with ContentState (trackedDriver, trackedPosition, trackedGap, currentLap, totalLaps, sessionStatus, isLeader)
  - RaceLiveActivityView Widget with Dynamic Island compact/expanded/minimal and lock screen banner
  - NSSupportsLiveActivities enabled on F1AI target
  - RaceLiveActivity.swift registered in both F1AI and F1AIWidgets targets

affects: [04-02-live-activity-lifecycle, 04-03-websocket-timing]

# Tech tracking
tech-stack:
  added: [ActivityKit]
  patterns: [Live Activity Widget declared as plain struct Widget added to WidgetBundle body, dual-target xcodegen source path for shared types]

key-files:
  created:
    - ios/F1AIWidgets/RaceLiveActivity.swift
  modified:
    - ios/F1AIWidgets/NextRaceWidget.swift
    - ios/project.yml
    - ios/F1AI.xcodeproj/project.pbxproj

key-decisions:
  - "RaceLiveActivity.swift placed in F1AIWidgets/ and added to F1AI target via xcodegen explicit source path with group: F1AI/Widgets — cleaner than a separate Shared/ directory"
  - "statusColor helper is a free function (not a method) scoped with private — avoids protocol conformance boilerplate for a pure utility"
  - "isLeader Bool included in ContentState for UI branching in expanded view — gap display can short-circuit to LEADER without string parsing"

patterns-established:
  - "Dual-target xcodegen: add explicit path entry to the non-owning target's sources array alongside the directory-based source"
  - "statusColor uses lowercased() string contains — case-insensitive and handles partial matches (e.g. 'safety car' within longer status strings)"

requirements-completed:
  - LIVE-01
  - LIVE-02

# Metrics
duration: 2min
completed: 2026-03-08
---

# Phase 4 Plan 01: ActivityKit Live Activity Declaration Summary

**RaceLiveActivityAttributes struct and Dynamic Island widget declared with lock screen banner, compact, expanded, and minimal views — xcodegen updated for dual-target membership and NSSupportsLiveActivities plist key**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-08T23:13:28Z
- **Completed:** 2026-03-08T23:14:51Z
- **Tasks:** 4 steps (1 atomic commit)
- **Files modified:** 4

## Accomplishments
- Declared `RaceLiveActivityAttributes` with `ContentState` fields needed by Dynamic Island views: driver abbreviation, position, gap, lap, total laps, session status, and leader flag
- Implemented `RaceLiveActivityView` Widget with lock screen banner (.activityBackgroundTint black) and full Dynamic Island surface (compact leading/trailing, minimal, expanded leading/trailing/bottom)
- Added `INFOPLIST_KEY_NSSupportsLiveActivities` and `INFOPLIST_KEY_NSSupportsLiveActivitiesFrequentUpdates` to the F1AI target in project.yml
- Configured xcodegen to compile `RaceLiveActivity.swift` into both `F1AI` and `F1AIWidgets` targets (confirmed in generated pbxproj: two distinct PBXBuildFile entries for same fileRef)
- Registered `RaceLiveActivityView()` in the `F1AIWidgets` WidgetBundle body alongside `NextRaceWidget()`

## Task Commits

All implementation steps committed atomically as one logical unit:

1. **Steps 1-4: RaceLiveActivity declaration, bundle registration, project.yml update, xcodegen regeneration** - `fa9cddd` (feat)

**Plan metadata:** (this SUMMARY commit)

## Files Created/Modified
- `ios/F1AIWidgets/RaceLiveActivity.swift` - ActivityAttributes struct + ContentState + RaceLiveActivityView Widget (lock screen + Dynamic Island)
- `ios/F1AIWidgets/NextRaceWidget.swift` - Added `RaceLiveActivityView()` to WidgetBundle body
- `ios/project.yml` - Added NSSupportsLiveActivities keys to F1AI target; added RaceLiveActivity.swift as explicit source for F1AI target with group F1AI/Widgets
- `ios/F1AI.xcodeproj/project.pbxproj` - Regenerated by xcodegen; dual PBXBuildFile entries for RaceLiveActivity.swift confirmed

## Decisions Made
- Placed `RaceLiveActivity.swift` in `F1AIWidgets/` (its primary home) and included it in `F1AI` target via an explicit xcodegen source path entry — avoids a third `Shared/` directory while satisfying dual-target membership
- `statusColor` implemented as a `private` free function using `lowercased()` + `contains()` — handles partial status strings (e.g. "Safety Car Deployed") without exact matching
- `isLeader: Bool` added to ContentState alongside `trackedGap: String` — lets the view branch on Boolean rather than parsing the gap string to detect leader status

## Deviations from Plan

None - plan executed exactly as written. xcodegen source path approach for dual-target membership was the straightforward interpretation of Step 4's instruction to add the file to both targets' PBXSourcesBuildPhase sections.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. Xcode project regenerated automatically via xcodegen.

## Next Phase Readiness

- `RaceLiveActivityAttributes` and `ContentState` types are available in both `F1AI` and `F1AIWidgets` compile units
- `RaceLiveActivityView` is registered in the WidgetBundle — Activity can be rendered once lifecycle wiring is complete
- Ready for 04-02: `LiveActivityService` can now call `Activity<RaceLiveActivityAttributes>.request(...)` and `.update(...)` without type-not-found errors
- No blockers

## Self-Check: PASSED

- RaceLiveActivity.swift: FOUND at ios/F1AIWidgets/RaceLiveActivity.swift
- NextRaceWidget.swift: FOUND (modified) at ios/F1AIWidgets/NextRaceWidget.swift
- project.yml: FOUND (modified) at ios/project.yml
- Commit fa9cddd: FOUND in git log

---
*Phase: 04-live-race-experience*
*Completed: 2026-03-08*
