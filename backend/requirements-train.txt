# Offline ML training + evaluation dependencies.
#
# The training/evaluation pipeline is now sourced entirely from the local f1db
# dataset, so it needs NONE of the API server's FastF1/LangChain/Chroma stack.
# This lean set keeps the scheduled retraining CI fast.
#
# Install with: pip install -r requirements-train.txt
pandas
numpy
scikit-learn
scipy
joblib
requests
structlog
