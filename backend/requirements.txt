# Web framework
fastapi
uvicorn[standard]

# LLM / AI orchestration (runtime only). PDF/document-loader deps used solely by
# the offline rulebook ingest live in requirements-ingest.txt so the web build
# stays lean.
langchain-core
langchain-groq

# Embeddings model (downloaded on first use) — powers rulebook + memory vectors.
# Rulebook vectors are stored in Postgres/pgvector (see app/rag/pgvector_store.py),
# so no separate vector-DB package is needed.
sentence-transformers

# F1 data
fastf1

# Web search
tavily-python

# MCP server (optional — for Claude Desktop integration)
fastmcp


# Structured logging
structlog

# Durable document store (Supabase / Postgres) — optional, JSON fallback if unset
psycopg[binary]

# Utilities
pydantic
requests
httpx
python-dotenv
pandas
scikit-learn
joblib
